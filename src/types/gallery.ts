export type GalleryObj = {
  fileFormat: string
  image: {
    byteSize: string
    height: number
    width: number
    thumbnailLink: string
    thumbnailHeight: number
    thumbnailWidth: number
  }
  link: string
  mime: string
}
